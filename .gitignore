.DS_Store
__pycache__/
*.pyc
deploy/
.claude/
trump_accounts.db
trump_accounts.db-shm
trump_accounts.db-wal
# employers_seed.sql and state_grants_seed.sql are tracked — they're the
# git-side source of truth for rebuilding the DB. zip_income_seed.sql stays
# ignored (too large; regenerable via census_import.py).
/zip_income_seed.sql
.wrangler/

# Local secrets for `wrangler pages dev` — MUST never be committed (public repo)
.dev.vars
.dev.vars.*

# Any local DB / SQLite dumps or a subscribers export
*.db
*.sqlite
*.sqlite3
subscribers*.sql
